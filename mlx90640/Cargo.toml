[package]
name = "mlx90640"
version = "0.1.0"
edition = "2024"
description = "no_std driver for the MLX90640 32x24 IR thermal camera with bad pixel correction"

[dependencies]
embedded-hal = "1.0"
libm = "0.2"
